# 07 测试体系

> **389<!--M=test_passed--> passed, 2<!--M=test_skipped--> skipped, 0<!--M=test_failed--> failed**（完整测试套件，~34 秒）

---

## 一、三层测试

| 层次 | 脚本 | 耗时 | 目的 |
|------|------|------|------|
| **L1: 冒烟** | `scripts/smoke_test.py` | ~15s | 快速验证安装正确 |
| **L2: 全量** | `python -m pytest tests/` | ~34s | 开发者/CI 验证 |
| **L3: 性能** | `scripts/bench_continuity.py` | 1-15min | 性能报告展示 |

---

## 二、冒烟测试（7 项）

| 测试项 | 验证内容 | 通过标准 |
|--------|----------|----------|
| T-M1 | 安装验证 | 核心文件无第三方依赖 |
| T-M2 | 进程启动 | Archiver 进程存活 |
| T-M3 | 写入事件 | 热轨目录有 JSONL 文件 |
| T-M4 | 归档性能 | 延迟 <5s，速率 ≥200 events/s |
| T-M5 | 完整性 | 哈希链覆盖率 ≥90% |
| T-M6 | Dashboard API | HTTP 200 + 合法 JSON |
| T-M7 | 干净退出 | exit_code=0 |

```bash
python scripts/smoke_test.py        # 完整测试
python scripts/smoke_test.py fast   # 快速模式（跳过 Dashboard）
```

---

## 三、性能基准

### 3.1 方案 A：15min × 1000/s

| 指标 | 结果 |
|------|------|
| 总 emit | 882,400 |
| 归档成功率 | **99.9%** |
| RSS | 17.7 → 18.2 MB |
| 磁盘 | 2.63GB |

### 3.2 方案 B：3min × 5000/s

| 指标 | 结果 |
|------|------|
| 总 emit | 883,700 |
| 归档成功率 | **100%** |
| RSS | 17.8 → 18.4 MB |
| 磁盘 | 1.12GB |

### 3.3 方案 C：120s × 1000/s + 实时分诊

| 指标 | 结果 |
|------|------|
| 总 emit | 120,000 |
| 丢事件率 | **0.04%** |
| RSS 基线 → 峰值 → 终值 | 29.0 → 54.1 → 45.4 MB |
| CPU 均值 | ~100%（1 核） |
| 分诊耗时 | 2,975 ms（12 万事件） |
| 生成诊断 | 45,645 条（14 轮分诊） |

### 3.4 方案 D：75s × 2000/s（15 万事件）

| 指标 | 结果 |
|------|------|
| 总发射 | 150,000 |
| 归档率 | **100%** |
| RSS 基线 → 终值 | **37.8 → 38.7 MB**（+0.9 MB，+2.4%） |
| DB 大小 | 72 MB |
| RSS 采样 | 全程 38.3~38.9 MB，零泄漏 |

```bash
python scripts/bench_continuity.py                     # 15min, 1000/s
python scripts/bench_continuity.py --rate 5000         # 15min, 5000/s
python scripts/bench_continuity.py --total 1000000     # 100 万事件
python scripts/bench_continuity.py --rate 5000 --duration 300  # 5000/s, 5min
```

---

## 四、测试文件（41 个）

| 测试文件 | 范围 |
|----------|------|
| `test_stress_short.py` | 短时压力测试 |
| `test_stress_150k.py` | 15 万事件压力测试 + 性能曲线 |
| `test_archiver*.py` | 归档器核心功能 |
| `test_probe*.py` | 探针功能 |
| `test_lit*.py` | 诊断插件 |
| `test_cli*.py` | CLI 命令 |
| ... | ... |

---

## 五、测试输出

| 文件 | 说明 |
|------|------|
| `reports/smoke_test_results.json` | 冒烟测试 JSON |
| `reports/smoke_test_report.html` | 冒烟测试可视化报告 |
| `reports/bench_report.html` | 基准测试可视化报告 |
| `reports/bench_data.jsonl` | 基准测试原始时间序列 |

---

## 六、CI 结果

**389<!--M=test_passed--> passed, 2<!--M=test_skipped--> skipped, 0<!--M=test_failed--> failed**（v0.11.9-alpha）

- 新增 203 个测试用例
- 覆盖 22 个模块（remedy_engine、anchor、fingerprint、otel_bridge、probe_platform 等）
- 含 3s × 500eps 短时压力测试

---

**测试哲学：三层测试（冒烟/全量/性能），389 项全量验证，15 万事件压力测试。**